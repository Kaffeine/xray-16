dist: trusty
sudo: required

language: cpp

compiler:
  - clang
  - gcc

before_install:
    - sudo apt-get -qq update
    - sudo apt-get install -y libsdl2-dev
    - sudo apt-get install -y libtheora-dev

before_script:
  - cd src
  - cmake .

script: make

notifications:
  email: false

os:
  - linux
