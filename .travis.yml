dist: trusty
sudo: required

language: cpp

compiler:
  - clang
  - gcc

before_install:
    - sudo apt-get -qq update
    - sudo apt-get install -y libtheora-dev

before_script:
  - cd src
  - cmake .

script: make

branches:
  only:
    - linux

notifications:
  email: false

os:
  - linux
